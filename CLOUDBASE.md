# 云开发部署说明

本项目通过云函数 `lessonService` 读写云数据库。用户身份从云函数的微信上下文获取，课程文档以 `ownerOpenid` 绑定所有者；小程序端不直接访问数据库。

## 首次配置

1. 在微信开发者工具顶部点击“云开发”，按向导创建或关联一个云开发环境。
2. 在项目设置中确认已使用环境 `cloud1-d9gv6il9s47674b85`。应用启动时会显式连接该环境。
3. 在资源管理器中右键 `cloudfunctions/lessonService`，选择“上传并部署：云端安装依赖”。
4. 在云开发控制台的数据库中确认生成了 `users` 和 `courses` 集合。它们会在首次登录或写入课程时自动创建；也可以手动先创建空集合。
5. 将数据库权限设为“仅云函数可读写”（或等效的拒绝客户端直接读写规则）。所有读写都应只经由 `lessonService` 云函数。

## 数据结构

`users`：微信身份对应的昵称、头像、手机号与创建时间。手机号仅在用户点击“授权手机号”后由云函数获取，页面仅显示脱敏结果。

`courses`：课程资料、消课记录、到期日和 `ownerOpenid`。云函数会在每次查询、修改和删除前按 `ownerOpenid` 校验所有权。

## 真机要求

- 头像、昵称和手机号授权应在真机上测试。
- 手机号授权按钮需要已认证的小程序主体及微信平台允许的手机号能力；开发者工具模拟器不能返回真实手机号。
- 课程数据会跟随同一微信账号。退出本机登录不会删除云端课程。
